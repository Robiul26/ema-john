import React from 'react';

const Inventory = () => {
    return (
        <div>
            <h2 className='text-center mt-5'>Developer is sleeping...</h2>
        </div>
    );
};

export default Inventory;